<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.gsein.system.mapper.SystemUserMapper">
    <!--通过用户名获取用户-->
    <select id="getUserByUsername" resultType="systemUser">
        select id, username, password, salt, mobile, dept_id, status
        from sys_user
        where username = #{username}
          and is_deleted = 0
    </select>

    <!--保存系统用户-->
    <insert id="save" parameterType="systemUser" useGeneratedKeys="true">
        insert into sys_user (username, password, salt, mobile, dept_id, status)
        values (#{username}, #{password}, #{salt}, #{mobile}, #{deptId}, #{status})
    </insert>

    <!--更新系统用户信息-->
    <update id="update" parameterType="systemUser">
        update sys_user
        <set>
            <if test="password!=null and password!=''">
                password = #{password},
            </if>
            <if test="salt!=null and salt!=''">
                salt = #{salt},
            </if>
            <if test="mobile!=null and mobile!=''">
                mobile = #{mobile},
            </if>
            <if test="deptId!=null and deptId!=''">
                dept_id = #{deptId},
            </if>
            <if test="status!=null and status!=''">
                status = #{status},
            </if>
        </set>
        where username = #{username} and is_deleted = 0
    </update>

</mapper>

